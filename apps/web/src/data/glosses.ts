/**
 * First practice vocabulary — deliberately aligned with the glosses Malkith's
 * 3D avatar can already play (SSL-Transformer/signs/*.json), so the
 * learn ↔ communicate integration demo works with zero extra data.
 */
export const SEED_GLOSSES = ['ME', 'YOU', 'NAME', 'WHAT', 'WHERE', 'CAN', 'YOUR'] as const
